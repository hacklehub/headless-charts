import React from 'react';
import { Meta, Story } from '@storybook/react';
import PieChart, { PieChartProps } from './PieChart'; // Import the PieChart component and its props interface

// Meta information about the story
export default {
  title: 'Components/PieChart', // This will be the title in the Storybook sidebar
  component: PieChart, // The component being showcased
  argTypes: {
    data: { control: 'array' }, // You can add more controls for other props if needed
  },
} as Meta;

// Create a default template for the component
const Template: Story<PieChartProps> = (args) => <PieChart {...args} />;

// Default story, you can customize it further with args
export const Default = Template.bind({});
Default.args = {
  data: [
    { name: 'Category A', value: 30 },
    { name: 'Category B', value: 40 },
    { name: 'Category C', value: 20 },
  ],
  id: 'default-chart',
  value: 'value',
  labels: { key: 'name' },
};
